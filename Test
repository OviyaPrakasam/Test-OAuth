LandingPageController

public ModelAndView doViewLanding(HttpServletRequest request, HttpServletResponse response) throws Exception {
			//code for HK T&C
			try {
				String tcJsonValue = (String) request.getSession().getAttribute(TC_JSON_VALUE);
				logger.debug("tcJsonValue==:{},getIsTCAccepted from customervo--"+tcJsonValue+customerVO.getIsTCAccepted());
				logger.debug("doViewLanding-TC_FLAG_ENABLE---"+request.getSession().getAttribute(TC_FLAG_ENABLE));
				if(StringUtils.isNotBlank(tcJsonValue) && StringUtils.isNotBlank(customerVO.getIsTCAccepted())
						&& (customerVO.getIsTCAccepted().equalsIgnoreCase(CommonConstant.CONST_N)
						|| customerVO.getIsTCAccepted().equalsIgnoreCase(CommonConstant.CONST_Y))) {
					/*ObjectMapper objectMapper = new ObjectMapper();
					JsonNode rootNode = objectMapper.readTree(tcJsonValue);
					JsonNode termsOuterArray = rootNode.path("terms");
					if(termsOuterArray.isArray() && !termsOuterArray.isEmpty()){
						JsonNode termInnerArray = termsOuterArray.get(0);
						for(JsonNode term :termInnerArray) {
							String termVersion = (String) term.path("term-version").asText();
							logger.debug("termVersion:{},customerVO.getTcVersionNo-:{}"+termVersion + customerVO.getTcVersionNo());
							if(((StringUtils.isNotBlank(termVersion)) && StringUtils.isBlank(customerVO.getTcVersionNo()))
									|| ((StringUtils.isNotBlank(termVersion)) && (StringUtils.isNotBlank(customerVO.getTcVersionNo())
									&& !customerVO.getTcVersionNo().equalsIgnoreCase(termVersion)))){
								session.setAttribute(TC_FLAG_ENABLE,CommonConstant.CONST_Y);
								session.setAttribute("termVersion",termVersion);
								break;
							}else{
								session.setAttribute(TC_FLAG_ENABLE,CommonConstant.CONST_N);
								break;
							}
						}
					}*/
					if(((StringUtils.isNotBlank(tcJsonValue)) && StringUtils.isBlank(customerVO.getTcVersionNo()))
							|| ((StringUtils.isNotBlank(tcJsonValue)) && (StringUtils.isNotBlank(customerVO.getTcVersionNo())
							&& !customerVO.getTcVersionNo().equalsIgnoreCase(tcJsonValue)))){
						session.setAttribute(TC_FLAG_ENABLE,CommonConstant.CONST_Y);
						session.setAttribute("termVersion",tcJsonValue);
					}else{
						session.setAttribute(TC_FLAG_ENABLE,CommonConstant.CONST_N);
					}
				}else{
					logger.debug("else block-tcJsonValue-->"+tcJsonValue);
					session.setAttribute(TC_FLAG_ENABLE, CommonConstant.CONST_Y);
				}
			} catch (Exception ex){
				logger.error("T&C flag checks"+ex.getMessage());
			}
}




		public void doGetTCList(HttpServletRequest request, HttpServletResponse response) {
		logger.debug("doGetTCList-tcJsonValue---"+request.getParameter(TC_JSON_VALUE));
		logger.debug("doGetTCList-txnVersion --"+request.getParameter("tncVersion"));
		if ((request.getParameter("tncVersion") != null || StringUtils.isNotBlank(request.getParameter(TC_JSON_VALUE))) && StringUtils.isBlank((String)request.getSession().getAttribute(TC_JSON_VALUE))) {
			request.getSession().setAttribute(TC_JSON_VALUE, request.getParameter(TC_JSON_VALUE));
			request.getSession().setAttribute(TC_FLAG_ENABLE,CommonConstant.CONST_N);
			logger.debug("TermsCondition JSON value has been attached to session");
		}
	}

	/**
	 * Update User table post T&C accepted by user
	 * @param request
	 * @param response
	 * @return
	 * @throws Exception
	 */
	public ModelAndView doTnCUpdate(HttpServletRequest request, HttpServletResponse response) throws Exception{
		logger.debug("doTnCUpdate-termsVersion --"+request.getParameter("termVersion"));
		logger.debug("LandingPageController-doTnCUpdate-txnVersion-"+request.getSession().getAttribute("termVersion"));
		HttpSession session = request.getSession(false);
		CustomerVO customerVO = getCustomerVO();
		customerVO.setIsTCAccepted(CommonConstant.CONST_Y);
		customerVO.setTcAcceptedTime(new Date());
		customerVO.setTcVersionNo(StringUtils.isNotBlank((String) session.getAttribute("termVersion"))?session.getAttribute("termVersion").toString():CommonConstant.PAD_ZERO);
		customerBO.updateTermsAndCondition(customerVO);
		getCustomerInfo().setCustomerVO(customerVO);
		return doViewLanding(request, response);
	}
	
	
	
	CustomerBO.Java
	
	 public void updateTermsAndCondition(CustomerVO customerVO) throws BusinessException;
	 
	 
	 
	CustomerBOImplementation.Java
	
	
	@Override
	public void updateTermsAndCondition(CustomerVO customerVO) throws BusinessException{
		if (customerVO == null) {
			throw new BusinessException("[updateTermsAndCondition: customerVO is null]");
		}
		try {
				customerService.updateTermsAndCondition(customerVO);

		} catch (Exception e) {
			handleException(e);
		}
	}
	
	
	
	CustomerDAO.Java
	

	/**
	 *  T&C updated
	 * @param customerVO
	 * @throws DAOException
	 */
	public void updateUserTnCInfo(CustomerVO customerVO) throws DAOException;
	
	
	CustomerDAOImplementation:
	
	
	/**
	 *  Terms and Condition CR
	 * @param customerVO
	 * @throws DAOException
	 */
	public void updateUserTnCInfo(CustomerVO customerVO) throws DAOException {
		logger.debug("DAO-updateUserTnCInfo -Start --");
		try {
		Query query = currentSession().createQuery(
				"update UserVO "
						+ "set tcAcceptedTime = :tcAcceptedTime, tcVersionNo = :tcVersionNo, "
						+ "isTCAccepted = :isTCAccepted, "
						+ "updatedDate = :updatedDate, updatedBy = :updatedBy "
						+ "where id = :id");

		query.setParameter("tcAcceptedTime", customerVO.getTcAcceptedTime());
		query.setParameter("tcVersionNo", customerVO.getTcVersionNo());
		query.setParameter("isTCAccepted", customerVO.getIsTCAccepted());
		query.setParameter("updatedDate", new Date());
		query.setParameter("updatedBy", SysParamConstant.GBL_APP_NAME);
		query.setParameter("id", customerVO.getId());
		int i=query.executeUpdate();
		logger.debug("DAO-updateUserTnCInfo -end --"+i);
		}catch(Exception e ){
			logger.error("ERROR IN DAO-updateUserTnCInfo :" + e.getMessage());
		}
	}
	
	
	
	CustomerService.Java
	/**
	 * T&C updates
	 * @param customerVO
	 * @throws BusinessException
	 */
	public void updateTermsAndCondition(CustomerVO customerVO) throws BusinessException;
	
	
	
	CustomerServiceImpl.Java
	
    /**
     * updateTermsAndCondition
     * @param customerVO
     * @throws BusinessException
     */
    public void updateTermsAndCondition(CustomerVO customerVO) throws BusinessException{
        if (customerVO == null) {
            throw new BusinessException("[updateTermsAndCondition: customerVO is null]");
        }
        try {
                customerDAO.updateUserTnCInfo(customerVO);

        } catch (DAOException e) {
            throw new BusinessException(e);
        }

    }
	UserVO.Java  (Entity class)
		
	public String getTcVersionNo() { return tcVersionNo; }
	public void setTcVersionNo(String tcVersionNo) { this.tcVersionNo = tcVersionNo; }
	public Date getTcAcceptedTime() { return tcAcceptedTime; }
	public void setTcAcceptedTime(Date tcAcceptedTime) { this.tcAcceptedTime = tcAcceptedTime; }
	public String getIsTCAccepted() { return isTCAccepted; }
	public void setIsTCAccepted(String isTCAccepted) { this.isTCAccepted = isTCAccepted; }

	
	SecurityServiceImpl.Java
	
	  userVO.setIsTCAccepted(CommonConstant.CONST_Y);
      userVO.setTcAcceptedTime(new Date());
      userVO.setTcVersionNo(ralDetailVO.getTcVersionNo());
				
	  customerService.updateUserVO(userVO);
	  
	  
	  
	RALDetailVO.Java
	
	private String tcVersionNo;
    public String getTcVersionNo() { return tcVersionNo; }
    public void setTcVersionNo(String tcVersionNo) { this.tcVersionNo = tcVersionNo; }
    
	
	OnlineRegistrationController.java
	
	       //TODOTAMIL JSON CONSUME
        String termVersion = null;
        String tcValue = (String) request.getSession().getAttribute("tcValue");
        logger.debug("OnlineRegister-doEntryCreateCredential-tcValue-"+tcValue);
        if(StringUtils.isNotBlank(tcValue)) {
            ObjectMapper objectMapper = new ObjectMapper();
            JsonNode rootNode = objectMapper.readTree(tcValue);
            JsonNode termsOuterArray = rootNode.path("terms");
            if (termsOuterArray.isArray() && !termsOuterArray.isEmpty()) {
                JsonNode termInnerArray = termsOuterArray.get(0);
                for (JsonNode term : termInnerArray) {
                    termVersion = (String) term.path("term-version").asText();
                    request.getSession().setAttribute("termVersion",termVersion);
                    logger.debug("OnlineRegister-doEntryCreateCredential-termVersion-"+termVersion);
                    break;
                }
            }
        }else{
            logger.debug("OnlineBankingRegister-else --");
            termVersion=CommonConstant.IBNK_TNC_DEFAULT_VERSION;
            request.getSession().setAttribute("termVersion",termVersion);
        }
		
		
		
		ralDetailVO.setTcVersionNo(StringUtils.isNotBlank(request.getSession().getAttribute("termVersion").toString())?request.getSession().getAttribute("termVersion").toString():CommonConstant.IBNK_TNC_DEFAULT_VERSION);
        


	CustomerVO.hbm.xml
	
	        <!-- T&C required for SCBCIF. TBL_CUST_SERV -->
        <property name="tcVersionNo" type="string" column="TC_VERSION_NO"/>
        <property name="tcAcceptedTime" type="timestamp" column="TC_ACCEPTED_TIME"/>
        <property name="isTCAccepted" type="string" column="TC_ACCEPTED_FLAG"/>
		
		
	UserVO.hbm.xml
	
	        <!-- T&C required for SCBCIF. TBL_CUST_SERV -->
        <property name="tcAcceptedTime" type="timestamp" column="TC_ACCEPTED_TIME"/>
        <property name="tcVersionNo" type="string" column="TC_VERSION_NO"/>
        <property name="isTCAccepted" type="string" column="TC_ACCEPTED_FLAG"/>



-----------------------------------------------------------------------------------------------------


include.Jsp

<script>
 var tcFlagEnable ="${tcFlagEnable}";
</script>


<c:set var="tcFlagEnable" value="${sessionScope.tcFlagEnable}"/>

<c:if test="${(tcFlagEnable == null or tcFlagEnable == '' or tcFlagEnable == 'Y')}">
    <script type="text/javascript">
                debugger;
               async function getTncVersionJSON() {
                    const response = await fetch('https://av.sc.com/configuration/hk-digital-banking-tnc-version-control.json');
                    return ((response.ok) ? response.json() : false);
                }
             async function postTncData() {
                var tncVersionData = await getTncVersionJSON();
                console.log(tncVersionData);
                var digitalTerms = tncVersionData.terms[0].filter(item=> {return item['term-id']=='con-tc001'})[0];
                console.log(digitalTerms['term-version']);
                var tncVersion = digitalTerms['term-version']
                var termVersion = digitalTerms['term-version']
                $.ajax({
                 url: "/hk/nfs/ibank/landing.htm",
                 type: "POST",
                 data: { tcJsonValue: termVersion, a:'doGetTCList?tncVersion'+tncVersion},
                 success: function(response) {
                   console.log('data persisted');
                   console.log(response);
                   return response;
                 },
                 error: function(xhr) {
                   console.log('1error');
                   console.log(xhr);
                   return null;
                 }
               });
            }
            postTncData();
          </script>
</c:if>


landing.jsp

<link rel="stylesheet" type="text/css" href="<spring:theme code="tncPopup"/>" media="all"/>
<script type="text/javascript" src="${contextPath}/js/pdf.min.js"></script>
<script  type="text/javascript" src="${contextPath}/js/pdf.worker.min.js"></script>
<script type="text/javascript" src="${contextPath}/js/tnc_pdf_download.js"></script>
<script type="text/javascript" src="${contextPath}/js/tnc_popup.js"></script>

	<c:if test="${sessionScope.tcFlagEnable == 'Y'}">
		<section id="sc-login-tc-popup"></section>
		<section id="sc-login-notice-popup">
			<!-- notice popup -->
			<div class="sc-login-notice-box__popup">
				<div class="sc-login-notice-box__popup-container">
					<div style="margin:12px;"><p class="sc-login-notice-alert_img"></p></div>
					<div style="font-size: 18px;color: #2772C7;">
						<spring:message code="accounts.overview.tncNotice.title"/>
					</div>
					<div id="noticeContent" style="font-size:14px!important;line-height: 20px;text-align: justify;padding: 30px;max-height: 68%;overflow: auto;"></div>
					<button onclick="agreeNotice()" style="margin-top: 5px;background: none;border: none;font-size: 18px;width: 92%;height: 42px;"><spring:message code="accounts.overview.tncNotice.okBtn"/></button>
				</div>
			</div>
		</section>

	</c:if>

		
	<section id="confirm-logout-popup" class="hide">
		<div class="confirm-logout-popup-box__popup">
			<div class="confirm-logout-popup-box__popup-container">
				<div style="margin:12px;"><p class="sc-login-notice-alert_img"></p></div>
				<div style="color: #2772C7;">
					<span style="font-size: 20px;"><spring:message code="accounts.overview.confirmLogoutPopup.title"/></span>
				</div>
				<p style="text-align: justify;padding: 20px;font-size: 12px;">
					<spring:message code="accounts.overview.confirmLogoutPopup.Content"/>
				</p>
				<div class="confirmLogout_btn_group">
					<button onclick="CancelLogout()" class="confirmLogout_btn"><spring:message code="accounts.overview.confirmLogoutPopup.cancel"/></button>
					<button onclick="Logout()" class="confirmLogout_btn"><spring:message code="accounts.overview.confirmLogoutPopup.agree"/></button>
				</div>
			</div>
		</div>
	</section>

	<script>
		async function getTncMaintenanceMsg(url) {
			const response = await fetch('https://av.sc.com/configuration/hk-digital-banking-tnc-update-notice.json');
			return ((response.ok) ? response.json() : false);
		}
		async function setNoticeContent() {
			var message = await getTncMaintenanceMsg();
			console.log(message);
			// var currentUserLang = userLang == 'zh_CN' ? 'cn' : userLang == 'zh_HK' ? 'zh' : 'en';
			var noticeContentMsg = message.filter(item => {return item.lang==userLang})[0].tncMaintenanceMessage;
			console.log(noticeContentMsg);
			console.log(document.getElementById('noticeContent'));
			document.getElementById('noticeContent').innerHTML = noticeContentMsg;
		}
		setNoticeContent();
		
		function agreeNotice() {
			document.getElementById('sc-login-notice-popup').classList.add('hide');
			tncPopup('sc-login-tc-popup', ['https://av.sc.com/hk/content/docs/hk-digital-tnc.pdf'],'',onTncAccept);
		}
		function Logout() {
			document.getElementsByClassName('sc-login-box__popup')[0].classList.remove('blank_bg');
			document.getElementById('sc-login-tc-popup').classList.add('hide');
			window.location = '${ibankPath}/ibank_logout.htm';
		}
		function CancelLogout() {
			document.getElementById('confirm-logout-popup').classList.add('hide');
		}
		function onTncAccept() {
			$.ajax({
				type:'POST',
				url:"${ibankPath}/landing.htm?a=doTnCUpdate",
				timeout:60000,
				success:function(status){},
				error:function(){},
			});
		}
	</script>


Main.jsp

var tcFlagEnable = ${tcFlagEnable};

